name: jev
description: Fast, type-safe decision routing (Choice, Score, Noul) via Jev model.
instructions: |
Use the Jev decision engine for low-latency classification, boolean triaging,
and agent tool routing instead of generating verbose conversational tokens.

Model: jev-latest

Output Formats: Boolean (Noul), Score (0-1), Choice (Enumeration)
